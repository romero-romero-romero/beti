import 'package:flutter_test/flutter_test.dart';

// TODO: Actualizar tests en Fase 6 cuando la app tenga UI completa.
// Por ahora este test es un placeholder que verifica que el framework funciona.
void main() {
  testWidgets('Betty app smoke test', (WidgetTester tester) async {
    // Placeholder — los tests reales requieren mockear Isar y Supabase.
    expect(1 + 1, equals(2));
  });
}
